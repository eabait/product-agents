{
  "name": "@product-agents/skills-intent",
  "version": "0.1.0",
  "description": "LLM-backed intent classification skill for artifact planning",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint 'src/**/*.{ts,tsx}'",
    "test": "node --test --loader ../../product-agent/tests/ts-loader.mjs tests/**/*.test.ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@product-agents/agent-core": "*",
    "@product-agents/openrouter-client": "*",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
